<UserControl
    x:Class="VPlayer.Library.Views.ArtistsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:VPlayer.Library.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mvvm="http://prismlibrary.com/"
    xmlns:controls="clr-namespace:VCore.Controls;assembly=VCore"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Grid>

        <controls:PlayableWrapPanel Margin="5,0" ItemsSource="{Binding View}" SnapsToDevicePixels="False">
            <controls:PlayableWrapPanel.ItemTemplate>
                <DataTemplate>
                    <controls:PlayableWrapPanelItem BottomText="{Binding BottomText}"
                                          HeaderText="{Binding Name}"
                                          ImageThumbnail="{Binding ImageThumbnail}"
                                          IsPlaying="{Binding IsPlaying}" />
                </DataTemplate>
            </controls:PlayableWrapPanel.ItemTemplate>
        </controls:PlayableWrapPanel>
    </Grid>
</UserControl>