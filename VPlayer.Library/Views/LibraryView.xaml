<UserControl x:Class="VPlayer.Library.Views.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:behaviors="clr-namespace:VCore.Behaviors;assembly=VCore"
             xmlns:controls="clr-namespace:VCore.Controls;assembly=VCore"
             xmlns:controls1="clr-namespace:WpfToolkit.Controls;assembly=VirtualizingWrapPanel"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:regions="http://prismlibrary.com/"
             xmlns:regions1="clr-namespace:VPlayer.Core.Modularity.Regions;assembly=VPlayer.Core"
             xmlns:design="clr-namespace:VCore.Design;assembly=VCore"
             xmlns:desing="clr-namespace:VPlayer.Library.Desing"
             d:DesignHeight="650"
             d:DesignWidth="800"
             Background="{StaticResource BackgroudPageBrush}"
             mc:Ignorable="d">

    <UserControl.Resources>
        <desing:LibraryRegionDesingProvider x:Key="LibraryRegionDesingProvider" />
    </UserControl.Resources>

    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>

        <Border Grid.RowSpan="2"
            Margin="0,0,5,0"
            BorderBrush="{StaticResource 10_WhiteTransparentBrush}">
            <ListView ItemsSource="{Binding NavigationViewModel.Items}" Style="{StaticResource ListView_MenuStyle}" />
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition />
            </Grid.RowDefinitions>

            <controls:Finder Grid.Row="0" Margin="5" />

            <Grid Grid.Row="1"
            behaviors:DropBehavior.PreviewDropCommand="{Binding FilesDropped}"
            AllowDrop="True">
                <ContentControl
                        regions:RegionManager.RegionName="{x:Static regions1:RegionNames.LibraryContentRegion}"
                        design:DesignTimeRegionProvider.ViewProvider="{StaticResource LibraryRegionDesingProvider}" />
            </Grid>
        </Grid>
    </Grid>
</UserControl>