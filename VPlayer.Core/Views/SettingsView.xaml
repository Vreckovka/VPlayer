<UserControl x:Class="VPlayer.Core.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VPlayer.Core.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">


  <UserControl.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/VPlayer.WindowsPlayer;component/Views/WindowsPlayer/WindowsPlayerViewResources.xaml" />
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </UserControl.Resources>


  <Border Padding="10">
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="auto"/>
        <ColumnDefinition Width="2"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
        <RowDefinition Height="auto"/>
        <RowDefinition Height="auto"/>
        <RowDefinition Height="auto"/>
      </Grid.RowDefinitions>

      <TextBlock Text="Delete all data"  Margin="0 0 5 0" VerticalAlignment="Center"/>
      <Grid Background="{StaticResource 10_WhiteTransparentBrush}" Grid.Column="1" Grid.RowSpan="2"/>
      <Button Width="150" Content="Delete all data"  Margin="5 5 5 5" Grid.Column="2" HorizontalAlignment="Left" VerticalAlignment="Center" Command="{Binding DeleteAllData}"/>


      <Grid Background="{StaticResource 10_WhiteTransparentBrush}" Grid.ColumnSpan="3" Height="2" VerticalAlignment="Top" Grid.Row="1"/>

    </Grid>
  </Border>
</UserControl>
