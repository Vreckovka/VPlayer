<UserControl x:Class="VPlayer.WindowsPlayer.Views.WindowsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls1="clr-namespace:VCore.Controls;assembly=VCore.WPF"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:mvvm="http://prismlibrary.com/"
             xmlns:regions="clr-namespace:VPlayer.Core.Modularity.Regions;assembly=VPlayer.Core"
             d:DesignHeight="850"
             d:DesignWidth="800"
             mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="470" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>


        <ListView Grid.Row="0"
                  Grid.ColumnSpan="2"
                  BorderThickness="0,0,0,2"
                  ItemsSource="{Binding NavigationViewModel.Items}"
                  Style="{StaticResource ListView_MenuStyle}">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>

        <Menu Grid.Row="0"
              Grid.Column="1"
              Margin="0,0,0,0"
              HorizontalAlignment="Left"
              Background="Transparent">
            <MenuItem Width="80"
                      Height="30"
                      Background="Transparent"
                      BorderThickness="0"
                      Header="Load">
                <MenuItem Command="{Binding LoadFromFolder}" Header="Music" />
                <MenuItem Command="{Binding LoadTvShow}" Header="Tv show" />
            </MenuItem>
        </Menu>


        <Border Grid.Row="1"
                Grid.Column="0"
                Grid.ColumnSpan="2"
                Padding="0 1 0 0">
            <ContentControl mvvm:RegionManager.RegionName="{x:Static regions:RegionNames.WindowsPlayerContentRegion}" />
        </Border>

    </Grid>
</UserControl>